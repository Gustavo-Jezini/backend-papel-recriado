PORT=3030
NODE_ENV=development
LOG_LEVEL=info

# PostgreSQL — use docker-compose credentials for local dev
# Note: port 5433 is used to avoid conflicts with local PostgreSQL instances on 5432
DATABASE_URL=postgresql://papel_recriado:papel_recriado_123@localhost:5433/papel_recriado

# JWT — generate a strong random string for production (min 32 chars)
JWT_SECRET=
JWT_EXPIRES_IN=7d
